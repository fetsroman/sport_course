<p><%= t 'mailer.hello' %> <%= @email %>!</p>

<p><%= t 'mailer.confirm' %></p>

<p><%= link_to (t 'mailer.button'), confirmation_url(@resource, confirmation_token: @token, locale: I18n.locale) %></p>
